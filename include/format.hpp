#pragma once

#include "include/lex.hpp"
#include "include/formalism.hpp"

std::string format_as(const RPE &);
std::string format_as(const token &);
std::string format_as(const std::vector <token> &);
